# Project Map

Silvershield is a Flask cybersecurity-training app. Users register, pass SMS two-factor login, take a pre-survey, practice scam detection in desktop, mobile, and phone role-play modules, then take a post-survey. Progress and difficulty live in SQLite. Scenario text is generated by Groq (`llama-3.1-8b-instant`). Speech uses gTTS. OTP uses Twilio Verify.

## How a user moves through the app

1. `GET /` — home page (`templates/homePage.html`)
2. `GET /account_creation` — register (`templates/accountCreation.html` + `static/auth.js`)
   - `POST /register` — creates the user, logs them in, sends them to the pre-survey
3. `GET /login` — login page (`templates/loginPage.html` + `static/auth.js`)
   - `POST /login` — checks password; seeded user `testuser` skips OTP; everyone else gets an SMS code
   - `POST /verify_otp` — checks the Twilio code
4. `GET/POST /pre_survey` — required before any module (`templates/survey.html`)
5. `GET /dashboard` — progress hub (`templates/dashboard.html` + `static/dashboard.js`)
6. Training modules (each simulation needs 5 logged attempts before that module counts as done):
   - `GET /module1` — desktop email + desktop browser (`templates/desktopPage.html`)
   - `GET /module2` — mobile email, SMS, call, web (`templates/MobilePage.html`)
   - `GET /phone_roleplay` — branching phone-scam role-play (`templates/phoneRoleplay.html`)
7. When all three modules are complete, `GET /dashboard` redirects to `GET/POST /post_survey`
8. After the post-survey, the dashboard stays available
9. `GET /logout` — clears the session, keeps the language choice

Difficulty is stored per user, per channel, on a 1–4 scale (easy, medium, hard, very hard). A correct answer raises that channel by 1 (capped at 4). A wrong answer resets it to 1.

Languages: English (`en`), Spanish (`es`), Chinese (`zh`). `GET /set_language/<lang>` stores the choice in the session and redirects back.

## File tree (what each file is for)

| File | Purpose |
| --- | --- |
| `app.py` | Flask app: pages, auth, surveys, Groq generators, scoring |
| `database.py` | Creates and migrates `silvershieldDatabase.db` |
| `user_login.py` | Register a user and check a password |
| `TWOFA.py` | Twilio Verify: send and check SMS codes |
| `metrics.py` | Log attempts, update progress, read dashboard stats |
| `survey_engine.py` | Build, validate, and save pre/post surveys |
| `survey_config.json` | Survey questions, scales, consent text |
| `survey_translations.json` | Spanish and Chinese strings for survey text |
| `multimodal.py` | `POST /tts` — text to MP3 via gTTS |
| `templates/*.html` | Pages the server renders |
| `static/*.js` | Browser behavior for each page |
| `static/*.css` | Page styles |
| `static/snippets/*.html` | Fake phone-app screens loaded into the mobile module |
| `translations/es/.../messages.po` | Spanish UI strings (Flask-Babel) |
| `translations/zh/.../messages.po` | Chinese UI strings |
| `messages.pot` | Babel message template |
| `babel.cfg` | Tells Babel to scan `.py` and templates |
| `requirements.txt` | Python packages |
| `render.yaml` | Render.com deploy (`gunicorn app:app`, Python 3.11.9) |
| `pytest.ini` | Test discovery |
| `tests/` | Automated tests |
| `.github/workflows/python-tests.yml` | Runs pytest on push and pull request |
| `.env.example` | Names of required secrets (do not commit a real `.env`) |
| `README.md` | Setup and deploy notes |
| `get-pip.py` | Vendored pip installer. Not part of the app. |

## Environment variables (read at startup by `app.py`)

| Variable | Purpose |
| --- | --- |
| `SECRET_KEY` | Flask session signing key |
| `GROQ_KEY` | Groq API key for generated scenarios and feedback |
| `TWILIO_ACCOUNT_SID` | Twilio account |
| `TWILIO_AUTH_TOKEN` | Twilio auth token |
| `TWILIO_VERIFY_SID` | Twilio Verify service |
| `DB_PATH` | Optional path to the SQLite file (default `silvershieldDatabase.db`) |
| `DB_DOWNLOAD_TOKEN` | Required query token for `GET /admin/download-db` |
| `PORT` | Local server port (default 5000) |
| `SURVEY_CONFIG_PATH` | Optional override for `survey_config.json` |
| `SURVEY_TRANSLATIONS_PATH` | Optional override for `survey_translations.json` |

## Database tables (created by `database.init_database`)

### `users`

Account plus six difficulty columns:

- `difficulty_email_desktop`
- `difficulty_internet_desktop`
- `difficulty_email_mobile`
- `difficulty_sms_mobile`
- `difficulty_call_mobile`
- `difficulty_web_mobile`

A seeded account is always written: username `testuser`, password `SilverShieldTest!1`.

### `scenario_attempts`

One row per training answer: type, platform, choice, correct answer, timing, AI feedback, message text.

### `critical_indicators`

Clue strings the user is credited with when they answer correctly.

### `performance_summary`

Running totals per user and scenario type: attempts, correct count, success rate, average time, indicator count.

### `module_progress`

How many scenarios are done for a module key. Cap is 5 (`REQUIRED_ATTEMPTS_PER_SIMULATION`).

### `pre_survey` / `post_survey`

Summary fields plus a full `response_json` blob.

### `system_usability_survey`

SUS questions 1–10 and the computed SUS score (written from the post-survey).

### `phone_roleplay_sessions`

One call practice: scenario, difficulty, start/end, score, outcome.

### `phone_roleplay_events`

Each choice, dialogue load, restart, and completion inside a session.

### `phone_roleplay_results`

Attempt count, hints used, indicators, behavior pattern, feedback text.

### `module_assessment_assignments` / `enrollments` / `results` / `scores`

Older A/B pre-post quiz data. The live routes no longer show these quizzes.

### `user_consents`

Analytics consent from the pre-survey.

### `survey_question_assignments`

Which knowledge questions were picked for this user and phase.

### `survey_responses`

One row per answered survey question.

Indexes: attempts by user+time, indicators by attempt, module progress, assessment lookups.

## `database.py` — functions

`_column_names(cursor, table_name)`
Returns column names via `PRAGMA table_info`.

`_table_exists(cursor, table_name)`
True if that table is in `sqlite_master`.

`_migrate_scenario_attempts(cursor)`
Creates `scenario_attempts`, or rebuilds it if old columns `attempt_number` or `critical_indicators_identified` still exist. Adds `message` if missing.

`_ensure_test_user(cursor)`
Inserts or updates the seeded `testuser` account with a hashed password.

`init_database()`
Opens `silvershieldDatabase.db`, creates every table above, adds missing columns, backfills `module_assessment_scores` from results, creates indexes, commits, closes. Also runs when the file is executed directly, and `app.py` calls it on import.

## `user_login.py` — functions

`connecting_to_database()`
Opens `silvershieldDatabase.db` with `sqlite3.Row` so columns can be read by name.

`user_registration(username, password, email, phone, address)`
Hashes the password and inserts a `users` row. Returns `(True, "User Registration Successful")` or `(False, error message)` on duplicate username/phone/email.

`verifying_login(usernameOrEmail, password)`
Looks up by username or email and checks the hash. Returns `(True, phone, username)` or `(False, None, None)`.

## `TWOFA.py` — functions

`_get_twilio_client_and_service()`
Reads Twilio env vars and returns `(Client, verify service SID)`. Raises if any var is missing.

`send_otp(phone)`
Starts an SMS verification to that number.

`verify_otp(phone, code)`
Returns True only if Twilio status is `"approved"`.

## `metrics.py` — functions

`_connect()`
SQLite connection with a 10-second lock timeout.

`log_scenario_attempt(...)`
Inserts a `scenario_attempts` row (computes duration from `start_time` if needed). Inserts each clue in `indicators_found` into `critical_indicators`. Creates or updates `performance_summary` (attempts, correct count, success rate, rolling average duration, indicator total).

`log_critical_indicators(username, scenario_attempt_id, scenario_type, indicators_found)`
Inserts indicator rows for an existing attempt. The main logger already does this itself.

`update_module_progress(username, module_name, scenarios_completed=None)`
Increments completed count by 1, or sets an explicit count. Never goes above 5. Updates `last_accessed`.

`get_user_performance(username)`
Returns `{success, data: [{scenario_type, total_attempts, correct_attempts, success_rate, avg_duration_seconds, critical_indicators_found}]}`.

`get_module_progress(username)`
Returns `{success, modules: [{module_name, completed, total, completion_percentage, last_accessed}]}`.

`get_attempt_history(username, scenario_type=None, limit=20)`
Newest attempts, optionally filtered by type.

`get_survey_comparison(username)`
Pre vs post module-assessment scores, plus `pre_survey` and `post_survey` summaries and `confidence_change`.

`get_learning_metrics(username)`
Total time, attempt counts, overall success rate, total indicators, and the six current difficulty levels.

## `survey_engine.py` — constants and functions

Constants:

- `SURVEY_PHASE_KEYS` — `"pre"` -> `preTrainingSurvey`, `"post"` -> `postTrainingSurvey`
- `SELF_EFFICACY_IDS` — `SE1`–`SE6`
- `USEFULNESS_IDS` — `PU1`–`PU3`
- `ENGAGEMENT_IDS` — `HM1`–`HM2`
- `SUS_IDS` — `SUS1`–`SUS10`

`load_survey_translations()`
Cached load of `survey_translations.json`. Empty dict if missing or broken.

`_active_survey_language()`
`"es"`, `"zh"`, or `"en"` from the current Babel locale.

`_translate_survey_text(text)`
Looks up the Spanish/Chinese string, then falls back to Flask gettext.

`load_survey_config()`
Cached load of `survey_config.json`.

`get_consent_config()`
The consent block (required yes/no before analytics).

`get_survey_config(phase)`
The pre or post survey object.

`get_survey_completion_table(phase)`
`"pre_survey"` or `"post_survey"`.

`_seeded_rng(*parts)`
A `random.Random` seeded from a SHA-256 of the parts, so the same user always gets the same question draw.

`_get_existing_assignments(cursor, username, phase)`
Rows already stored in `survey_question_assignments`.

`_ensure_section3_assignments(cursor, username, phase, section)`
For knowledge section S3, picks `questionSelectionCount` questions per subsection (currently 1). Post-survey avoids the question ids already used in that user's pre-survey for the same subsection. Does nothing if assignments already exist.

`ensure_phase_assignments(db_path, username, phase)`
Runs the S3 picker and commits.

`_decorate_question(question, inherited_scale=None, subsection=None)`
Copies a question, fills type, translated scale labels, text, options, required, `fieldName`, subsection id and topic.

`_assignment_id_set(...)`
Ordered question ids assigned to one subsection.

`build_survey_view_model(db_path, username, phase)`
The object `templates/survey.html` renders: title, description, sections, and consent on the pre-survey only. S3 shows only assigned questions. Other sections show every question.

`_translate_consent(consent)`
Translates prompt, details, and button labels.

`_iter_questions(sections)`
Yields every question, including ones nested in subsections.

`_coerce_answer(form_data, question)`
`multiChoice` uses `getlist`; everything else is a stripped string.

`validate_submission(form_data, survey_model)`
Requires consent `"yes"` when consent is required. Requires every required question. Returns `(answers, None)` or `(None, error message)`.

`_numeric_average(answers, question_ids)`
Mean of integer answers, or None.

`_compute_sus_score(answers)`
Standard SUS: odd items contribute `(value - 1)`, even items contribute `(5 - value)`, then multiply by 2.5. None if any SUS item is blank.

`_json_value(value)`
JSON-encodes lists; leaves other values as-is.

`_upsert_pre_summary(cursor, username, answers)`
Writes `pre_survey`: age=`D1`, device=JSON of `D6`, confidence=average of `SE1`–`SE6`, plus full `response_json`.

`_upsert_post_summary(cursor, username, answers)`
Replaces `post_survey`: confidence (SE average), perceived usefulness (PU average), `behavior_change` JSON of `BI1`–`BI5`, `recommendation_likelihood`=`HM2`, `learning_rating`=HM average, full `response_json`.

`_upsert_usability_summary(cursor, username, answers)`
Replaces `system_usability_survey` with `SUS1`–`SUS10` and the SUS score.

`save_survey_submission(db_path, username, phase, survey_model, answers)`
Upserts analytics consent, replaces `survey_responses` for that phase, then writes the pre summary or the post summary plus SUS score.

## Survey content (`survey_config.json`)

### Consent

Required yes/no before survey and analytics collection.

### Pre-training sections

- **S1** Demographics and Technology Background (`D1` age, `D2` sex, `D3` education, `D4` home language, `D5` internet hours, `D6` devices)
- **S2** Technology Proficiency
- **S3** Cybersecurity Knowledge Test — 1 random question from each subsection: `PHISHING_EMAIL_1`, `VISHING`, `SOCIAL_ENGINEERING`, `PASSWORD_SECURITY`, `RED_FLAGS`, `PRIZE_SCAMS`, `ONLINE_SAFETY`, `IDENTITY_THEFT`, `SAFE_BROWSING`, `ACCOUNT_SECURITY`
- **S4** Cybersecurity Self-Efficacy (`SE1`–`SE6`)
- **S5** Behavioral Intention (`BI1`–`BI5`)

### Post-training sections

- **S3** Same knowledge subsections, but a different question than the one shown pre
- **S4** Self-efficacy again
- **S5** Behavioral intention again
- **S6** Technology Acceptance: `PU` perceived usefulness, `EU` ease of use, `HM` hedonic motivation
- **S7** Usability and Accessibility: SUS (10 items) and `AC` accessibility items

## `multimodal.py` — functions

`tts()` — `POST /tts`

Body: `{text, lang, slow}`. Chinese is forced to `zh-CN`. Returns `{success, audio: base64 mp3, mime: "audio/mpeg"}`. 400 if text is empty, 500 if gTTS is missing or generation fails.

## `app.py` — startup

Loads `.env`, creates the Flask app, sets the secret key, calls `init_database()`, attaches Babel, registers the multimodal blueprint.

`MODULE_ROUTE_MAP`

- `desktop` -> `module1`
- `mobile` -> `module2`
- `phone` -> `phone_roleplay`

`MODULE_PROGRESS_REQUIREMENTS` (each key needs 5 attempts)

- `desktop`: `email_desktop`, `internet_desktop`
- `mobile`: `email_mobile`, `sms_mobile`, `call_mobile`, `web_mobile`
- `phone`: `phone_roleplay`

`MODULE_ASSESSMENT_QUESTION_BANKS`

Static real/fake quiz items for desktop, mobile, and phone. The routes that used to show them now redirect to the dashboard. `MODULE_ASSESSMENT_QUESTION_BANK` is an alias of the desktop bank (kept for tests).

## `app.py` — helpers

`select_locale()`
Session language, else the browser's best match among `en`, `es`, `zh`. Babel uses this.

`get_llm_output_language()`
`"Spanish"`, `"Chinese"`, or `"English"` for Groq prompts.

`is_model_refusal(text)`
True if the model reply looks like a refusal. Generators then use a built-in fallback.

`_get_module_question_bank(module_name)`
Returns that module's quiz list, or the desktop bank.

`_get_module_question_map(module_name)`
`{question id: question}`.

`_get_module_redirect(module_name)`
Redirect to that module's page, or `/dashboard`.

`_is_module_phase_complete(username, module_name, phase)`
True when every assigned pre or post quiz question has a result row.

`_get_or_create_module_variant(username, module_name)`
Stable A or B from a hash of username and module name. Stored in `module_assessment_enrollments`.

`_balanced_pre_question_ids_for_seed(module_name, seed_value)`
Splits the bank in half and searches up to 250 shuffles so pre and post have similar average difficulty (within 0.20).

`_ensure_module_question_assignments(username, module_name)`
Writes pre/post assignment rows. If the stored count does not match the bank size, it deletes old assignments and results and reassigns.

`_get_module_phase_questions(username, module_name, phase)`
Assigned questions for that phase, shuffled.

`_build_module_assessment_status(username)`
Per-module flags: variant, `pre_complete`, `post_complete`, `training_complete`, `post_available`, and URLs.

`_require_module_pretest(module_name)`
Redirects to login, pre-survey, or the module pre-test. Returns None if the pre-test is done. The live module pages do not call this. They only require the pre-survey.

`_is_module_training_complete(username, module_name)`
True when every required simulation key has at least 5 completed scenarios.

`_next_incomplete_training_target(username)`
First unfinished module's route name and label. Falls back to the dashboard route.

`_build_module_posttest_context(username, module_name)`
Whether to show a post-test button and which URL "back" should use. Not used by the current page routes.

`_has_completed_survey(username, table_name)`
True if that user has a row in `pre_survey` or `post_survey`.

`_all_module_assessments_complete(username)`
Returns `(all pre and post quizzes done, status list)`.

`_all_modules_training_complete(username)`
Returns `(all three training modules done, status list)`.

`_require_pre_survey_completion(username)`
Redirects to `/pre_survey` with a flash if the pre-survey is missing. Otherwise None.

`_module_back_url(username)`
`/post_survey` if every module is done and the post-survey is not. Otherwise `/dashboard`.

`inject_template_helpers()`
Gives every template `get_locale`.

`_is_safe_redirect_target(target_url)`
Allows only same-site paths that start with `/`. Blocks full URLs.

`get_difficulty(category)`
Reads one difficulty column for the logged-in user. Returns 1 if logged out or missing. `category` is a column name such as `difficulty_email_desktop`.

`set_difficulty(category, level)`
Clamps level to 1–4 and saves it for the logged-in user.

## `app.py` — page routes

`GET /` — `home()`
Renders `homePage.html`.

`GET/POST /pre_survey` — `pre_survey()`
Login required. If already done, goes to the dashboard. POST validates and saves, then goes to the dashboard.

`GET/POST /pre_survey_demographics` — `pre_survey_demographics()`
Login required, then always redirects to `/pre_survey`.

`GET /login` — `login()`
Renders `loginPage.html`.

`GET /account_creation` — `account_creation()`
Renders `accountCreation.html`.

`GET /dashboard` — `dashboard()`
Login required. Redirects to pre-survey if that is unfinished. If all training is done and the post-survey is not, redirects to the post-survey. Otherwise renders `dashboard.html` with a "continue module" or "training complete" call to action.

`GET/POST /post_survey` — `post_survey()`
Login required. Redirects to the dashboard if already done, or if training is not finished. POST validates and saves (including SUS), then goes to the dashboard.

`GET/POST /system_usability_survey` — `system_usability_survey()`
Usability is now inside the post-survey. This route only flashes that and redirects.

`GET /reset_presurvey` — `reset_presurvey()`
Drops the `pre_survey` table. Dev/admin utility. Not login-gated.

`GET /admin/download-db` — `download_database()`
403 unless `?token=` matches `DB_DOWNLOAD_TOKEN`. Sends the SQLite file as a download.

`GET /module1` — `module1()`
Desktop simulator. Login and pre-survey required. Renders `desktopPage.html`.

`GET /module2` — `module2()`
Mobile simulator. Login and pre-survey required. Renders `MobilePage.html`.

`GET /phone_roleplay` — `phone_roleplay()`
Phone role-play. Login and pre-survey required. Renders `phoneRoleplay.html`.

`GET /module_assessments` — `module_assessments()`
Flash: assessments removed. Redirects to the dashboard.

`GET/POST /module_assessment/<module_name>/<phase>` — `module_assessment()`
Same redirect. The quiz UI is not shown.

`GET /logout` — `logout()`
Clears the session, restores language, redirects to login.

`GET /set_language/<lang>` — `set_language(lang)`
Stores `en`, `es`, or `zh`, then redirects to a safe `?next=` path, the referrer, or home.

`GET /save_progress` — `save_progress()`
Flashes "Progress saved successfully!" and renders the dashboard template without the usual dashboard context.

## `app.py` — analytics API (login required; 401 if not)

| Route | Function | What it returns |
| --- | --- | --- |
| `GET /api/user_performance` | `api_user_performance()` | `metrics.get_user_performance` |
| `GET /api/module_progress` | `api_module_progress()` | `metrics.get_module_progress` |
| `GET /api/attempt_history` | `api_attempt_history()` | Query: `type`, `limit` (default 20) |
| `GET /api/survey_comparison` | `api_survey_comparison()` | `metrics.get_survey_comparison` |
| `GET /api/learning_metrics` | `api_learning_metrics()` | `metrics.get_learning_metrics` |

## `app.py` — auth API

`POST /register` — `register()`
Form: `username`, `password`, `email`, `phone`, `address`. On success sets session username and returns `redirect_url` `/pre_survey`.

`POST /login` — `login_post()`
Form: `username` (username or email), `password`. On success sets session username. `testuser` returns `twilio_bypassed` and does not send SMS. Others prefix `+1` if needed and call `send_otp`.

`GET /check_username` — `check_username()`
`?username=` -> `{exists}`

`GET /check_email` — `check_email()`
`?email=` -> `{exists}`

`POST /send_otp` — `send_otp_route()`
Form `phone`. Prefixes `+1`. Sends Twilio SMS.

`POST /verify_otp` — `verify_otp_route()`
`testuser` always succeeds. Otherwise checks phone + code with Twilio.

## `app.py` — phone role-play API

`POST /generate-roleplay-line` — `generate_roleplay_line()`
Body: `scenario_type`, `prompt_key`, `difficulty`. Asks Groq for 1–3 sentences of scammer dialogue in the user's language.

Scenario types: `bank_fraud`, `tech_support`, `grandparent`, `government`.

Prompt keys: `opening`, `ask_for_code`, `push_for_account_number`, `urgency_if_call_later`, `freeze_account_pressure`, `fake_name_and_department`, `remote_access_request`, `fake_system_alert`, `family_emergency_opening`, `family_pressure`, `government_threat_opening`, `gift_card_payment`, `refuse_documentation`.

`POST /phone-roleplay/start-session` — `start_phone_roleplay_session()`
Login required. Inserts `phone_roleplay_sessions` and an empty `phone_roleplay_results` row. Returns `session_id`.

`POST /phone-roleplay/log-event` — `log_phone_roleplay_event()`
Body: `session_id`, `event_type`, `event_value`, `time_offset_seconds`. Inserts `phone_roleplay_events`.

`POST /phone-roleplay/finish-session` — `finish_phone_roleplay_session()`
Closes the session (time, outcome, score, `completed=1`), updates results, then `update_module_progress(..., "phone_roleplay")`.

`GET /phone-roleplay/progress-summary` — `phone_roleplay_progress_summary()`
Login required. Overall completed count, average score, average time, plus the same grouped by scenario type.

## `app.py` — scenario generators

All of these are POST, all call Groq, and all have hardcoded fallbacks.

`POST /generate-email` — `generate_email()`
Body `platform`: `"mobile"` or `"desktop"` (default desktop). Coin-flip scam vs legitimate. Reads that platform's email difficulty. Returns `{success, email: HTML, expected_label: "scam"|"not_scam"}`.

`POST /api/generate_sites` — `generate_sites()`
Desktop browser. Reads `difficulty_internet_desktop`.

Inner helpers:

- `infer_brand_details(search_query)` — brand name and legit domain from the query
- `safe_extract_json(raw_text)` — strip fences and `json.loads`
- `normalize_result(parsed, fallback)` — title, url, description, `site_type`, `is_sponsored`
- `generate_one(prompt_text, fallback)` — up to 2 Groq tries, else fallback

`mode "list"` (default): 5 shuffled results (sponsored legit, sponsored phishing, two legit, one phishing). `mode "open"`: HTML mock page for the clicked result. Phishing pages are training mockups with placeholder fields. Other modes: 400.

`POST /generate-sms` — `generate_sms()`
Coin-flip scam vs legitimate SMS. Reads `difficulty_sms_mobile`. Optional `theme` (default bank / package / delivery / login security). Returns `{success, difficulty, expected_label, sms: {number, text, time, clues}}`.

`POST /generate-call` — `generate_call()`
Coin-flip scam vs legitimate call transcript. Reads `difficulty_call_mobile`. Optional `theme` (default government agency or tech support). Returns `{success, difficulty, expected_label, call: {number, caller_name, transcript, clues}}`.

`POST /generate-web` — `generate_web()`
Mobile browser. Reads `difficulty_web_mobile`.

Inner helpers:

- `safe_extract_json(raw_text)`
- `normalize_item(item, fallback_type)` — title, url, snippet, `site_type`
- `build_fallback_items()` — 2 ads and 6 mixed legit/phishing results

`mode "open"`: HTML for the clicked result. `mode "search"` (default): `{ads, results, pagination, clues}`. Query defaults to `"bank account security tips"`.

## `app.py` — scoring

`POST /api/analyze` — `analyze_email()`
Desktop email scoring. If the body includes `"type"`, it forwards to `analyze_any()`. If `expected_label` is present, correctness is computed locally and Groq only writes the explanation and clues. Otherwise Groq decides correct/incorrect. Updates `difficulty_email_desktop`, logs scenario type `"email"` / platform `"desktop"`, updates module progress `"email_desktop"`. Returns `{success, feedback, difficulty_now}`.

`POST /api/analyze_website` — `analyze_website()`
Desktop site scoring. Ground truth is `site_type` from generation (phishing vs legit). Groq writes explanation and clues. Updates `difficulty_internet_desktop`. Logs scenario type `"internet"` / platform `"desktop"`, module progress `"internet_desktop"`.

`POST /api/analyze_any` — `analyze_any()`
Shared scorer for email, internet, sms, call, and web. Body: `type`, `user_choice` (`scam`/`not_scam`/`fake`/`real`), `message`, optional `platform`, `expected_label`, `time_spent_seconds`. Mobile email uses `difficulty_email_mobile`. Other types use the map in the function. Same pattern: `expected_label` makes scoring local; otherwise Groq judges. Logs scenario type `"{type}_{platform}"` and updates module progress with that same key. Returns `{success, feedback, difficulty_now}`.

`if __name__ == "__main__":`
Runs the dev server on `0.0.0.0`, `PORT` or 5000, debug off. Production uses `gunicorn app:app` (see `render.yaml`).

## Templates

`templates/homePage.html`
Landing page. Language links, Log In, Register. CSS: `style.css`, `landingStyle.css`.

`templates/loginPage.html`
Username/email, password, then OTP slide. CSS: `style.css`, `landingStyle.css`, `loginStyle.css`. Script: `auth.js`.

`templates/accountCreation.html`
Multi-step registration: username, password, email, phone + OTP, address. CSS: `style.css`, `accountStyle.css`. Script: `auth.js`.

`templates/survey.html`
Renders whatever survey object the server passed (pre or post): consent, sections, Likert scales, single and multi choice. CSS: `style.css`, `landingStyle.css`, `dashboard.css`. Small inline script for form behavior. Language links and logout.

`templates/dashboard.html`
Module cards linking to `/module1`, `/module2`, `/phone_roleplay`, plus the continue/complete call to action. Inline script sets `dashboardLabels`. Script: `dashboard.js`. CSS: `style.css`, `landingStyle.css`, `dashboard.css`.

`templates/desktopPage.html`
Fake desktop with an email window and a browser window. Real/Fake buttons, read-aloud, voice answer. Inline script sets `desktopLabels`. Scripts: `tts.js`, `voiceInput.js`, `desktopEmail.js`, `desktopInternet.js`, `notification.js`. CSS: `style.css`, `desktopStyle.css`.

`templates/MobilePage.html`
Fake phone home screen. Apps load snippets. Inline script sets `mobileLabels`. Scripts: `tts.js`, `voiceInput.js`, `script.js`. CSS: `style.css`, `mobileStyle.css`.

`templates/phoneRoleplay.html`
Four scenario buttons and the call UI. Inline script builds `window.roleplayI18n` (translated strings). Script: `phoneRoleplay.js`. CSS is mostly in the template plus `style.css`.

`templates/systemUsabilitySurvey.html`
Old standalone SUS form with its own `<style>`. The route no longer renders it. SUS is section S7 of the post-survey.

## Static HTML snippets

Fetched by `script.js` as `/static/snippets/{type}.html`.

`static/snippets/email.html`
Fake Gmail screen. Back calls `backToApps()`.

`static/snippets/sms.html`
Fake Messages screen. Fills sender, avatar, and body.

`static/snippets/call.html`
Fake phone-call screen. Fills caller, number, transcript.

`static/snippets/web.html`
Fake Chrome screen. Search box, results, and opened page.

## Static JavaScript

### `static/auth.js`

Used by both login and registration. Elements that are missing on a page are simply skipped.

`showSlide(slideNumber)`
Shows one registration step.

`showLoginSlide(slideNumber)`
Shows one login step (credentials, then OTP).

`errorElement(input)`
Finds the error text element next to a field.

`validateUsername(usernameValue)`
Required, format check, then `GET /check_username`. Rejects names that already exist.

`validatePassword(passwordValue)`
Required and strength rules before the form can continue.

`validateEmail(emailValue)`
Format check, then `GET /check_email`.

`validatePhone(phoneValue)`
Required phone format check.

`validateStreet(streetValue)` / `validateCity(cityValue)` / `validateZip(zipValue)`
Required address-part checks.

`validateState(stateValue)`
State field check.

`validateAddress()`
Runs street, city, state, and zip together.

Event wiring (`DOMContentLoaded` and button listeners):

- Next/back move slides.
- Login Next posts `/login`. If `otp_sent`, shows the OTP slide. If `twilio_bypassed`, goes to the dashboard flow.
- Login verify posts `/verify_otp`.
- Registration Send OTP posts `/send_otp`. Verify posts `/verify_otp`.
- Submit posts `/register` and follows `redirect_url`.

### `static/dashboard.js`

`formatScenarioName(rawName)`
Turns keys like `email_desktop` into a readable label.

`formatSeconds(totalSeconds)`
Turns seconds into a short duration string.

`escapeHtml(value)`
Escapes text before it is inserted as HTML.

`fetchJson(url)`
GET JSON helper for the analytics APIs.

`renderOverview(metrics)`
Fills the summary cards from `/api/learning_metrics`.

`renderPerformance(items)`
Fills the per-scenario performance table from `/api/user_performance`.

`renderProgress(modules)`
Fills module completion bars from `/api/module_progress`.

`renderAttempts(attempts)`
Fills recent attempts from `/api/attempt_history`.

`renderDifficulty(items)`
Fills current difficulty levels from the learning-metrics payload.

`loadDashboardAnalytics()`
Calls those four APIs and renders them. Runs on page load.

### `static/desktopEmail.js`

Runs on `DOMContentLoaded`. Inner functions:

`loadEmail()`
`POST /generate-email` with platform desktop. Puts HTML in the mail pane, stores `expected_label`, preloads TTS.

`sendUserAnswer(choice)`
`POST /api/analyze` with the email HTML, choice, `expected_label`, and seconds spent. Shows `showNotification`, then loads the next email.

Click handlers: mail icon opens the window and loads an email; Real/Fake and the mic call `sendUserAnswer`; Read aloud plays preloaded TTS or `speak()`.

### `static/desktopInternet.js`

`stopCurrentTTS()`
Calls global `stopTTS` if it exists.

`htmlToPlainText(html)`
Strips tags for speech.

`getDesktopSearchQuery()`
Address-bar text, or `"security alerts"`.

`setDesktopInternetState({view, inputValue, metaText})`
Switches between search-results view and opened-site view. Shows the back button only on a site.

`renderDesktopSearchResults(results, query)`
Draws the result list. Each row can be opened.

`generateDesktopFakeSites(query)`
`POST /api/generate_sites` mode `"list"`. Renders results.

`openDesktopWebsite(site)`
`POST /api/generate_sites` mode `"open"`. Shows the mock page and enables Real/Fake.

`analyzeDesktopWebsite(choice)`
`POST /api/analyze_website` with the HTML, `site_type`, choice, and time spent. Shows the notification.

Search button, Enter, back, Real, Fake, mic, and read-aloud are wired at the bottom of the file.

### `static/script.js` (mobile module)

`applyMobileTranslations()`
Writes `mobileLabels` into Gmail, Chrome, SMS, and call chrome (buttons, search chips, footer).

`buildWebSpeechText(sc)`
Builds a speakable string from a search result or opened page.

`htmlToPlainText(html)`
Strips tags.

`ScenarioEngine.load(type)`
Fetches `/static/snippets/{type}.html` into `#scenarioBody`. Web calls `setupWebSearchUI` instead of generating one message. Email POSTs `/generate-email` (platform mobile). SMS POSTs `/generate-sms`. Call POSTs `/generate-call`. Stores `expected_label` and message text, preloads TTS, wires Scam / Not scam / mic / read-aloud.

`ScenarioEngine.setupWebSearchUI()`
Binds search, Enter, and back-to-results. Hides answer buttons until a site is open.

`ScenarioEngine.loadWebSearch(query)`
`POST /generate-web` mode `"search"`, then `renderMobileWebResults`.

`ScenarioEngine.openWebResult(site)`
`POST /generate-web` mode `"open"`. Shows the page and the answer buttons. Sets `expected_label` from `site_type`.

`ScenarioEngine.renderMobileWebResults(sc)`
Draws ads, results, and pagination.

`ScenarioEngine.fill(type, sc)`
Puts generated email HTML, SMS fields, call transcript, or web payload into the snippet.

`backToApps()`
Clears the scenario, shows the app grid, stops TTS.

`analyzeChoice(choice)`
For web, refuses to score until a result is opened. `POST /api/analyze_any` with platform `"mobile"`, then shows feedback and can load the next scenario.

### `static/phoneRoleplay.js`

`t(s)`
Looks up `window.roleplayI18n`, otherwise returns the English key.

`scenarios`
Four scripted trees. Each node has a `prompt_key` (sent to Groq) or is an ending with title, feedback, and consequence.

- `bank_fraud` — Bank Fraud Dept. Difficulty 1, 3 critical indicators.
- `tech_support` — Tech support / virus call.
- `grandparent` — Family-in-trouble call.
- `government` — Threats and payment pressure.

Endings include safe hang-up, unsafe disclosure, and partial recovery. Score and outcome are sent when an ending node is reached.

`postJSON(url, body)`
POST JSON and return the parsed body.

`startRoleplaySession(scenarioKey)`
`POST /phone-roleplay/start-session`. Stores session id and start time.

`logRoleplayEvent(eventType, eventValue)`
`POST /phone-roleplay/log-event` with seconds since start.

`finishRoleplaySession(node)`
`POST /phone-roleplay/finish-session` with outcome, score, attempts, indicators, hint use, and feedback text.

`getRoleplayLine(scenarioType, promptKey, difficulty)`
`POST /generate-roleplay-line`. Returns the spoken line, or a fallback string if the request fails.

`getRoleplayTTSLang()`
`es`, `zh-CN`, or `en` from the page lang attribute.

`speakText(text, lang, slow)`
`POST /tts` and play the mp3. Used for dialogue and ending feedback.

`loadScenario(key)`
Fills caller name, number, avatar, and hint. Starts a session, logs `scenario_started`, renders the start node.

`renderNode(nodeKey)`
Ending nodes show feedback, speak it, log completion, and finish the session. Other nodes load a Groq line, speak it, and build a button per choice. Each click logs `option_selected` and renders the next node.

Button wiring: scenario buttons call `loadScenario`; speak button replays the current line; restart logs `restart_clicked`, starts a new session, and returns to the start node.

### `static/tts.js`

`_normalizeText(raw)`
Collapses whitespace.

`getTTSLang()`
Page language as `en`, `es`, or `zh-CN`.

`getSpeechLocale(langCode)`
BCP-47 locale for the browser speech fallback (`en-US`, `es-ES`, `zh-CN`).

`stopTTS()`
Pauses server audio and cancels `speechSynthesis`. Keeps any preloaded clip.

`preloadTTS(text, options)`
Asks `/tts` ahead of time and stores one `HTMLAudioElement`.

`playPreloaded(text)`
Plays that clip if the text matches. Returns whether it played.

`speak(text, options)`
Stops current speech, tries `/tts`, and falls back to `window.speechSynthesis`. These are exposed on `window` so the desktop and mobile pages can call them.

### `static/voiceInput.js`

If the browser has no `SpeechRecognition`, it hides every `.voice-answer-btn`.

`getVoiceLocaleConfig()`
Recognition language and real/fake word lists for `en`, `es`, and `zh`.

`window.startVoiceAnswer(buttonEl, onReal, onFake)`
Listens once. Matches the transcript against real words (`real`, `safe`, `seguro`, `真实`, ...) or fake words (`fake`, `scam`, `estafa`, `诈骗`, ...). English words are always included in addition to the page language. Calls `onReal` or `onFake`. Shows "Listening..." on the button while active.

### `static/notification.js`

`showNotification(isCorrect, message, target)`
`target` `"email"` uses the email popup; anything else uses the internet popup. Adds class `correct` or `wrong`, sets the title to Correct! or Incorrect, shows for 5 seconds.

## Static CSS

| File | Purpose |
| --- | --- |
| `static/style.css` | Shared layout, type, and buttons |
| `static/landingStyle.css` | Home page and shared headers |
| `static/loginStyle.css` | Login slides |
| `static/accountStyle.css` | Registration steps |
| `static/dashboard.css` | Dashboard cards and survey layout |
| `static/desktopStyle.css` | Desktop email and browser windows |
| `static/mobileStyle.css` | Phone frame and the four app screens |

## Tests

`tests/conftest.py`

`test_db_path(tmp_path)`
A temporary SQLite path.

`app_client(monkeypatch, test_db_path)`
Points `database`, `metrics`, `user_login`, and hardcoded `"silvershieldDatabase.db"` connects at that file. Reloads `app.py`. Stubs `send_otp`. `verify_otp` succeeds only for code `123456`. Yields `(Flask test client, app module, db path)`.

`tests/test_database.py`

- `test_init_database_creates_expected_tables`
- `test_init_database_seeds_default_test_user`

`tests/test_metrics.py`

- `test_log_scenario_attempt_updates_performance_and_indicators`
- `test_update_module_progress_tracks_completion`
- `test_update_module_progress_caps_at_total`

`tests/test_app_routes.py`

Helpers: `_seed_user`, `_seed_pre_survey`, `_complete_module_pretest`, `_survey_answer_for`, `_build_survey_submission`.

Covers: home page, performance API auth, register+login, testuser OTP bypass, module access before and after the pre-survey, assessment routes redirecting to the dashboard, back-button target, dashboard redirect to post-survey when training is done, dashboard redirect to pre-survey, Spanish mobile labels, Chinese desktop labels, pre-survey save and required consent, post-survey save and SUS score, post-survey using different S3 questions, phone role-play auth and session start, `generate_sites` and `generate_web` result lists, fallbacks when Groq refuses or returns no choices.

Run: `pytest`

CI: `.github/workflows/python-tests.yml` on `ubuntu-latest`, Python 3.11.

## What calls what

Browser pages:

- `auth.js` -> `/check_username`, `/check_email`, `/register`, `/login`, `/send_otp`, `/verify_otp`
- `dashboard.js` -> `/api/user_performance`, `/api/module_progress`, `/api/attempt_history`, `/api/learning_metrics`
- `desktopEmail.js` -> `/generate-email`, `/api/analyze`, `/tts`
- `desktopInternet.js` -> `/api/generate_sites`, `/api/analyze_website`, `/tts`
- `script.js` -> `/static/snippets/*.html`, `/generate-email`, `/generate-sms`, `/generate-call`, `/generate-web`, `/api/analyze_any`, `/tts`
- `phoneRoleplay.js` -> `/generate-roleplay-line`, `/phone-roleplay/*`, `/tts`
- `voiceInput.js` -> browser SpeechRecognition only (no server)

Those routes then call:

- `user_login.py` — password hash and `users` table
- `TWOFA.py` — Twilio
- `survey_engine.py` — `survey_config.json` and survey tables
- `metrics.py` — attempts, indicators, `performance_summary`, `module_progress`
- Groq chat API — scenario text and feedback explanations
- gTTS — `/tts` audio
